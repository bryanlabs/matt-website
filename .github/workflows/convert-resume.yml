name: Convert Resume to DOCX

on:
  push:
    branches:
      - main
    paths:
      - 'resume.html'

jobs:
  convert:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install python-docx beautifulsoup4 lxml

      - name: Convert HTML to DOCX
        run: python scripts/build-resume-docx.py

      - name: Commit DOCX if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add resume.docx
          git diff --cached --quiet || git commit -m "[skip ci] Update resume.docx from resume.html"
          git push
